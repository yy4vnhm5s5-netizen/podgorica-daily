# Modules

Each future capability owns its presentation, application, domain, and infrastructure code beneath this directory. Modules may depend on `shared` contracts but must not import another module's internal implementation or persistence details.
